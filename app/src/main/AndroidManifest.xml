<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frn.frnstore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FrnStore"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".feature.listProduct.ListProductActivity"
            android:exported="false" />
        <activity
            android:name=".feature.product.comment.CommentsActivity"
            android:exported="false" />
        <activity
            android:name=".feature.product.ProductDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".feature.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>